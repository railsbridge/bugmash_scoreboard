<h2>August 2009 BugMash participants</h2>

<p>
  Need to report a <%= link_to 'missing contribution', new_issue_path %>.
</p>

<table>
  <tr>
    <th>Name</th>
    <th>Score</th>
  </tr>

<% @participants.each do |participant| %>
  <tr>
    <td><%= link_to(h(participant.name), participant) %></td>
    <td><%= participant.score %></td>
  </tr>
<% end %>
</table>
